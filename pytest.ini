[pytest]
markers =
    slow: starts a real Ray cluster; excluded by default, run with -m slow
# The default run excludes slow tests; a command-line -m (e.g. -m slow)
# overrides this because the last -m option wins.
addopts = -m "not slow"
